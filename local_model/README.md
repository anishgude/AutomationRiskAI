# Local Neural Baseline

This is a CPU-only neural network baseline that runs offline. It provides a point of comparison against the fine-tuned LLM without any external dependencies.

## Run
```powershell
python local_model/train.py
python local_model/evaluate.py
```
